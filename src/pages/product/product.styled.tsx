/**
 * Product based styled components
 */
import styled from "styled-components";

export const ProductMainContainer = styled.div`
  width: 100%;
  /* padding: ${({ theme }) => `${theme.spacing[64]} ${theme.spacing["128"]}`}; */
  `